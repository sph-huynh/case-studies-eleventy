---
label: Feature Content
hide_body: true
fields:
- name: featureContentTitle
  type: text
  config:
    required: false
  label: Title
- name: featurecontent
  type: list
  config:
    use_select: true
    min: 1
    max: 1
    source:
      type: pages
      section: feature
  label: FeatureContent
