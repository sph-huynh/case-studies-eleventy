---
label: Period Table
hide_body: true
fields:
- name: periodTitle
  type: text
  config:
    required: false
  label: Period Table Page Title
- name: perLoop
  type: field_group_list
  fields:
  - name: perTitle
    type: text
    config:
      required: false
    label: Table Category
  - name: perDesc
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: true
      schema:
        format: html
  config:
    min: 
    max: 
    labelField: perDescription
  label: Period Table  desc
